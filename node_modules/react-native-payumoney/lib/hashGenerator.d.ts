import { ValidatorInterface } from './interfaces';
declare const _default: ({ key, amount, email, txnId, productName, firstName, salt, udf1, udf2, udf3, udf4, udf5, udf6, udf7, udf8, udf9, udf10 }: ValidatorInterface) => string;
export default _default;
